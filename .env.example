# === App ===
APP_ENV=development
APP_DEBUG=true
APP_SECRET_KEY=change-me-to-a-strong-random-secret-at-least-32-chars
APP_BASE_URL=http://localhost:8000
WEB_BASE_URL=http://localhost:3000

# === Database ===
# POSTGRES_USER/PASSWORD is the Postgres bootstrap superuser (created by the
# postgres Docker image). It must NEVER be what the running app connects as:
# Postgres does not enforce Row-Level Security for superuser connections, so
# using it here would silently disable tenant isolation. See
# docs/architecture.md "Multi-tenant izolasyon".
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=leadpulse
POSTGRES_USER=leadpulse
POSTGRES_PASSWORD=leadpulse_dev

# Restricted, non-superuser role the running API/worker processes use.
# Created by migration 0ba5bf6fe643_create_restricted_app_role; its password
# must match LEADPULSE_APP_DB_PASSWORD below (change both together).
LEADPULSE_APP_DB_PASSWORD=leadpulse_app_dev
DATABASE_URL=postgresql+asyncpg://leadpulse_app:leadpulse_app_dev@localhost:5432/leadpulse

# Superuser connection string — used ONLY to run `alembic upgrade head`
# (DDL + role/grant management). Falls back to DATABASE_URL if unset, but in
# a properly isolated setup DATABASE_URL is the restricted role above, so set
# this explicitly whenever you run migrations.
MIGRATIONS_DATABASE_URL=postgresql+asyncpg://leadpulse:leadpulse_dev@localhost:5432/leadpulse

# === Redis / Celery ===
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# === Private campaign import storage ===
# Docker Compose uses these as MinIO server/API credentials in development.
# Production values must be unique secrets; do not expose MinIO on a public
# port and do not configure a browser presigned-upload flow in this slice.
MINIO_ROOT_USER=leadpulse_minio_dev
MINIO_ROOT_PASSWORD=change-me-to-a-long-unique-minio-secret
MINIO_BUCKET=leadpulse-imports

# === JWT ===
JWT_ALGORITHM=HS256
JWT_ACCESS_TTL_MINUTES=15
JWT_REFRESH_TTL_DAYS=7

# === CORS ===
CORS_ORIGINS=http://localhost:3000

# Only the immediate reverse proxy networks which are allowed to supply
# X-Forwarded-For. Leave empty when API traffic is direct; never trust client
# supplied XFF by default. Production must enable the four bounded private API
# limits (login, chat turn, workflow action, campaign import).
TRUSTED_PROXY_CIDRS=
API_RATE_LIMIT_ENABLED=false

# API/worker object-storage connection. In Docker use minio:9000; set true
# only behind TLS for a remote private MinIO endpoint.
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=leadpulse_minio_dev
MINIO_SECRET_KEY=change-me-to-a-long-unique-minio-secret
MINIO_SECURE=false

# Concrete first canary for CSV/XLSX outreach. Keep disabled until Meta
# sandbox validation succeeds. When enabled, name exactly one tenant and one
# sales manager UUID and leave the recipient cap at 100.
CAMPAIGN_IMPORTS_ENABLED=false
CAMPAIGN_IMPORTS_CANARY_TENANT_IDS=
CAMPAIGN_IMPORTS_CANARY_MANAGER_IDS=
CAMPAIGN_IMPORTS_MAX_RECIPIENTS=100

# === External APIs (fill in later) ===
WHATSAPP_APP_SECRET=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_BUSINESS_ACCOUNT_ID=
WHATSAPP_GRAPH_API_VERSION=v20.0
WHATSAPP_AGENT_SLUG=

GOOGLE_PLACES_API_KEY=
SERPAPI_KEY=
BING_SEARCH_KEY=

IYS_API_URL=
IYS_API_KEY=

# === Web crawler (Phase D) === off by default; see docs/architecture.md
# "Web crawler (Phase D)" before configuring seed URLs.
WEB_CRAWL_ENABLED=false
WEB_CRAWL_SEED_URLS=
WEB_CRAWL_PROXIES=

# === LLM provider ===
# Supported: ollama, chat_compatible, mock (development only).
# ``chat_compatible`` works with self-hosted vLLM, LocalAI, llama.cpp and
# similar Chat Completions servers on any operating system or in a container.
LLM_PROVIDER=
LLM_API_KEY=
LLM_MODEL=
# For Qwen/vLLM conversational serving: LLM_ENABLE_THINKING=false
# Ollama: http://host:11434 (or /v1); chat_compatible: https://host/v1
LLM_BASE_URL=http://localhost:11434/v1

# === Observability ===
SENTRY_DSN=
LOG_LEVEL=INFO

# === SMTP (dev uses mailhog) ===
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@leadpulse.local

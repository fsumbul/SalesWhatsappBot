# LeadPulse Web

Next.js 15 (App Router) frontend for the LeadPulse platform.

## Development

```bash
pnpm install
pnpm dev
```

The app runs at http://localhost:3000 and defaults to the Turkish locale (`/tr`).

Supported locales: `tr`, `en`, `de`, `ar`, `ru`.

# Application
APP_ENV=development
APP_DEBUG=true
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
APP_SECRET_KEY=change-me-please-min-32-chars-long-secret
APP_BASE_URL=http://localhost:8000
WEB_BASE_URL=http://localhost:3000

# Database
# DATABASE_URL must be the restricted, non-superuser `leadpulse_app` role —
# Postgres never enforces Row-Level Security for a superuser connection, so
# using the bootstrap superuser here would silently disable tenant isolation.
# The role is created by migration 0ba5bf6fe643_create_restricted_app_role;
# its password must match LEADPULSE_APP_DB_PASSWORD (change both together).
# See docs/architecture.md "Multi-tenant izolasyon".
LEADPULSE_APP_DB_PASSWORD=leadpulse_app_dev
DATABASE_URL=postgresql+asyncpg://leadpulse_app:leadpulse_app_dev@localhost:5432/leadpulse

# Superuser connection string — used ONLY to run `alembic upgrade head`
# (DDL + role/grant management, including creating the role above). Falls
# back to DATABASE_URL if unset.
MIGRATIONS_DATABASE_URL=postgresql+asyncpg://leadpulse:leadpulse_dev@localhost:5432/leadpulse

# Redis / Celery
REDIS_URL=redis://localhost:6379/0
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# JWT
JWT_ALGORITHM=HS256
JWT_ACCESS_TTL_MINUTES=15
JWT_REFRESH_TTL_DAYS=7

# CORS (comma-separated)
CORS_ORIGINS=http://localhost:3000

# WhatsApp Cloud API (Meta)
WHATSAPP_APP_SECRET=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_VERIFY_TOKEN=set-to-any-random-string-you-configure-in-meta
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_BUSINESS_ACCOUNT_ID=

# Discovery data sources
GOOGLE_PLACES_API_KEY=
SERPAPI_KEY=
BING_SEARCH_KEY=

# İYS (Turkey commercial-message registry)
IYS_API_URL=
IYS_API_KEY=

# Observability
SENTRY_DSN=
LOG_LEVEL=INFO

# SMTP (for invitations, alerts)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@leadpulse.local
